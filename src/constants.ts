// INFO: when some "constant" value is used more than once, it's always better to put it somewhere like here where it can be quickly and easily changed, rather than going to every single file it is placed in and then changing it there
export const AUTH_TYPES = {
  OAUTH2: "OAUTH2",
  LOCAL: "LOCAL",
};
